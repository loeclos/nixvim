{
  plugins.noice = {
    enable = true;

    settings = {
      popupmenu = {
        backend = "nui";
      };

      notify = {
        enabled = true;
      };

      presets = {
        bottom_search = false;
        command_palette = true;
      };
    };
  };
}
