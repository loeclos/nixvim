{
  plugins.noice = {
    enable = true;

    settings = {
      popupmenu = {
        backend = "nui";
      };

      presets = {
        bottom_search = false;
        command_palette = true;
      };
    };
  };
}
